# Copy to .env and fill in real values. NEVER commit .env (gitignored).

POSTGRES_USER=stockapp
POSTGRES_PASSWORD=change-me
POSTGRES_DB=stockapp

# python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=

# pgcrypto key for personal financial data (NFR-05) — same generator as above.
APP_ENCRYPTION_KEY=

# Single-user login. Hash via: python scripts/hash_password.py
ADMIN_USERNAME=jacob
ADMIN_PASSWORD_HASH=
