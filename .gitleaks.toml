# gitleaks config — stock-analysis-app (owner: A7 DevOps, P1-SEC-2 / task #7)
# Scope narrowed per A8 review 2026-07-08: allow ONLY the reviewed test dummy
# and .env.example placeholders; test fixtures otherwise remain scanned.

title = "stock-analysis-app gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Test-only non-secrets (reviewed A8 2026-07-08)"
paths = [
  '''\.env\.example''',
]
regexes = [
  '''(0123456789abcdef){4}''',  # TEST_JWT_SECRET dummy (tests/conftest.py) — exact 64-hex, not real
]
